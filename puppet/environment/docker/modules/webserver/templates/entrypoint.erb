#!/bin/bash

## local variables
GUNICORN_BIND="$1"
GUNICORN_PORT="$2"
GUNICORN_WORKERS="$3"
GUNICORN_TYPE="$4"

gunicorn \
    -b "$GUNICORN_BIND:$GUNICORN_PORT" \
    --workers=$GUNICORN_WORKERS \
    "factory:create_app(args={'instance': '$GUNICORN_TYPE'})"
