## @.scss-lint.yml, this file overrides the default configurations:
#
#      https://github.com/brigade/scss-lint/blob/master/config/default.yml
linters:
  Indentation:
    severity: warning
    width: 4

  SpaceAfterPropertyColon:
    enabled: true
    style: no_space # or 'one_space', or 'at_least_one_space', or 'aligned'